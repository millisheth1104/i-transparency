<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="{{ settings.color_background }}">
    <link rel="canonical" href="{{ canonical_url }}">
    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,400;1,9..144,500&family=Mulish:wght@300;400;500;600&family=Outfit:wght@400;500;600&display=swap">

    {{ content_for_header }}

    {{ 'base.css' | asset_url | stylesheet_tag }}
  </head>

  <body class="template-{{ request.page_type | handle }}">
    {% section 'header' %}

    <script>
      (function () {
        function setHeaderHeightVar() {
          var header = document.querySelector('[data-header]');
          var announcement = document.querySelector('[data-announcement-bar]');
          var h = (header ? header.getBoundingClientRect().height : 0) + (announcement ? announcement.getBoundingClientRect().height : 0);
          document.documentElement.style.setProperty('--header-height', h + 'px');
        }
        setHeaderHeightVar();
        window.addEventListener('resize', setHeaderHeightVar);
        window.addEventListener('load', setHeaderHeightVar);
        var header = document.querySelector('[data-header]');
        if (header && 'ResizeObserver' in window) {
          new ResizeObserver(setHeaderHeightVar).observe(header);
        }
      })();
    </script>

    <main id="MainContent" class="content-for-layout" role="main">
      {{ content_for_layout }}
    </main>

    {% section 'footer' %}

    {%- if request.page_type == 'index' -%}
      {% render 'whatsapp-float' %}
    {%- endif -%}
    {% render 'popup' %}

    <script src="{{ 'wishlist.js' | asset_url }}" defer></script>
    <script src="{{ 'scroll-reveal.js' | asset_url }}" defer></script>
  </body>
</html>
